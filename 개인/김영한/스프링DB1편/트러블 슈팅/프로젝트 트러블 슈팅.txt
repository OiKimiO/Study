1. settings
  1) Gradle 버전 에러 발생
   - 경로 
       * build.gradle
   - 문제점 
       (1) Type Could not run phased build action using Gradle installation
             An exception occurred applying plugin request 
        [id: 'org.springframework.boot', version: '2.6.4'] Failed to apply plugin 
        [id 'org.springframework.boot'] Spring Boot plugin requires Gradle 6.8.x, 
        6.9.x, or 7.x. The current version is Gradle 6.3
        * org.springframework.boot 2.6.4가 지원하는 그래들 버전이 현재 버전과는 일치 X
          # 현재 버전은 Gradle 6.3
          # 해당 에러 내용은 Problems에서 확인함
       (2) Could not fetch model of type 'BuildEnvironment' using Gradle 
        installation 'C:\Gradle\gradle-7.6.1' The specified Gradle installation 
        directory 'C:\Gradle\gradle-7.6.1' does not appear to contain a Gradle 
        distribution.
        * gradle-7.6.1폴더에 Gradle Distribution 설정이 포함되지 않았다는 에러
   - 대처
    (1) 해당 프로젝트만 그래들 버전을 업그레이드
      * https://gradle.org/releases/에서 7.6.1 다운로드 중
    (2) 압축을 풀고 C:\Gradle\gradle-7.6.1\gradle-7.6.1으로 구성되어 있어 
        gradle-7.6.1폴더를 밖으로 보내어 문제 해결

  2) JDBC 커넥션이 이루어지지 않음
     - 경로 
       * DBConnectionUtilTest
   - 문제 
       (1) Connection 정보 연결이 되지않고 Exception 처리됨
            - H2. 데이터베이스를 실행하지 않음
     - 대처 
       (1) \h2-2019-10-14\h2\bin\h2.bat 실행

  3) ResultSet이 null
      - 경로 
         * MemberRespositoryVO
    - 문제 
         * ResultSet의 값이 null로 나옴
       - 대처
         * PrepareStatement의 결과를 ResultSet 변수에 입력